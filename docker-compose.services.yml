version: "3.9"

services:
  notification-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: notification-service
    ports:
      - "3000:3000"
    env_file:
      - .env
    networks:
      - opsmind-net
    restart: unless-stopped

networks:
  opsmind-net:
    external: true
